-title "comments"
.comments
  -if @comments.empty?
    .alert
      暂无跟贴.
  -else
    %ul.comments
      -@comments.each do |c|
        %li{:class=>c.spam? ? "spam" : nil}
          %cite #{c.author_name}
          %date.muted=link_to c.created_at.to_formatted_s(:db),"/goto/#{c.commentable_type}/#{c.commentable_id}",:rel=>"nofollow"
          %p #{c.body}
=paginate @comments          

