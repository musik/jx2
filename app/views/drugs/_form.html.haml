= simple_form_for(@drug) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :en
    = f.input :category_id,:collection=>Category.select([:id,:name]).all.collect{|r| [[r.id,r.name].join('-'),r.id]}

  .form-actions
    = f.button :submit
